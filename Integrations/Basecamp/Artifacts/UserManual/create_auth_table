CREATE TABLE authorized (id INT NOT NULL AUTO_INCREMENT, 
    email VARCHAR(30), 
    access_token VARCHAR(255) NOT NULL, 
    refresh_token VARCHAR(255) NOT NULL,
    expires_in VARCHAR(16) NOT NULL,
	authenticated boolean NOT NULL,
    PRIMARY KEY (ID));